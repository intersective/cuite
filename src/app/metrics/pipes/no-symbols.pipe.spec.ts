import { NoSymbolsPipe } from './no-symbols.pipe';

describe('NoSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new NoSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
